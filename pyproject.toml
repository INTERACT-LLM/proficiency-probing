[project]
name = "proficiency-probing"
version = "1.0.0"
description = "Replication of paper: Multilingual Embedding Probes Fail to Generalize Across Learner Corpora"
readme = "README.md"
requires-python = ">=3.12.11"
dependencies = [
    "datasets>=4.5.0",
    "mord>=0.7",
    "numpy>=2.4.2",
    "scikit-learn>=1.8.0",
    "seaborn>=0.13.2",
    "torch>=2.10.0",
    "tqdm>=4.67.3",
    "transformers>=5.1.0",
]
